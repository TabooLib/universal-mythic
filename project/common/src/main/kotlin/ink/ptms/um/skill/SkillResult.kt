package ink.ptms.um.skill

enum class SkillResult {
    SUCCESS,
    ERROR,
    REQUIRES_PREMIUM,
    INVALID_VERSION,
    INVALID_TARGET,
    INVALID_CONFIG,
    MISSING_COMPATIBILITY,
    CONDITION_FAILED
}
